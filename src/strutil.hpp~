/*
===============================================================================
* File: strutil.hpp
*
* Author: Andrew Killian
*
* Email: Inceniium@gmail.com
*
* Description: Useful string functions that aren't part of the default c++
*              string class
===============================================================================
*/

#ifndef STRUTIL_HPP
#define STRUTIL_HPP

#include <string>
#include <vector>

/*
 * Name: stringSplit
 *
 * parameter 1: The string to be split
 *
 * parameter 2: The delimiter used to separate the string, defaults to
 *              whitespace
 *
 * Returns: A vector filled with fields (tokens) from the split string
 *
 * Description: Splits string into fields (tokens) based on a given delimiter
 */

std::vector<std::string> stringSplit(std::string toSplit, std::string delim=" ");

#endif
